#mongodb yaml file for kubernetes 
apiVersion: v1
kind: Pod
metadata:
  name: mongodb
  labels:
    app: mongodb
    tier: backend
spec:
  containers:
    - name: mongodb
      image: mongo
      ports:
        - containerPort: 27017
      envFrom:
        - secretRef:
            name: mongodb-secret